# Brain JS (Scrimba course)

## How they learn - Structure

```JS
// inputs & outputs
(inputs) => outputs;

// random values
Math.random();

// activation "relu"
function relu(value) {
  return value < 0 ? 0 : value;
}

```